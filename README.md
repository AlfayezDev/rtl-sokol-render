# Odin + Sokol Playground

Hot-reloading game dev setup in Odin using Sokol for graphics.

## Run

```bash
odin build builder.odin -file -out:builder && ./builder
```

Press `R` to hot-reload, `ESC` to quit.
